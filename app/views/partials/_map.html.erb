

<div id="mapid">



</div>



<script>
    const mymap = L.map('mapid').setView([37.0902, -95.7129], 5)
    
    const attribution =  '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors';
    const tileURL = 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png';
    const tiles = L.tileLayer(tileURL, {attribution})
    tiles.addTo(mymap)
    const geo = ['.../assets/javascripts/US_states.js']
    L.geoJSON(geo).addTo(mymap)

    // L.geoJson(US_states, {
    //     style: function(feature) {
    //         switch (feature.properties.name) {
    //             case 'Minnesota': return { color: '#ff0000'};
    //             case 'Colorado': return { color: '#ff0000'};
    //             default: return { color: '#0000ff'};
    //         }
    //     }
    // })

</script>